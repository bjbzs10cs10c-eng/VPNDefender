# VPNDefender 产品需求文档（PRD）

> 版本：v1.0  
> 状态：需求已确认，待开发  
> 编写日期：2026-08-29

## 1. 项目背景与目标

用户希望降低在学校（校园网）环境使用 VPN 被检测的风险。核心目标是：在用户点击“启动”某一 VPN 软件时，软件先检查当前所连接的 WiFi 名称；如果该名称命中“校园网黑名单”，则自动断开该网络并提醒用户，同时 **不启动** VPN 程序，从而避免在校园网下启动 VPN 被学校检测。

## 2. 目标用户

- 需要在校园网环境下临时绕开检测、但仍可能误用 VPN 的高校学生/教职工。

## 3. 核心功能

### 3.1 VPN 软件管理

支持管理多个 VPN 软件条目，每个条目包含：

- **显示名称**：用户可自定义（例如 “RabbitPro”）。
- **exe 完整路径**：包含 exe 文件名（例如 `D:\rabbitpro\RabbitPro.exe`），路径和 exe 名均可手动输入、修改。

界面操作：

- 添加：新增一个 VPN 条目，填写显示名称与完整 exe 路径。
- 修改：编辑已有条目的名称或路径。
- 删除：移除一个 VPN 条目。
- 选择：从列表中选中一条，作为“启动”的默认目标。

### 3.2 网络黑名单管理

黑名单用于维护“禁止在其下启动 VPN”的网络名称集合。默认包含：

- `UIBE-WLAN`

界面操作：

- 添加：新增一个网络名称。
- 删除：移除一个网络名称。
- 黑名单为所有 VPN 条目 **全局共用**，不区分条目。

### 3.3 启动逻辑

点击“启动”按钮后的处理流程：

1. 读取电脑**当前连接的 WiFi SSID**。
2. 判断该 SSID 是否命中黑名单：
   - **命中**：断开当前 WiFi → 弹窗提示 **“不要通过校园网使用vpn，已为您断开校园网。”** → 用户点击“确定”后回到主界面 → **不启动** exe。
   - **未命中** 或 **当前未连接任何 WiFi（无 SSID）**：正常启动选中的 exe。

> 说明：本软件为 **纯拦截/提醒** 逻辑（方案 A）。当检测到处于校园网时只做“断开 + 提醒 + 不启动”，不会自动切换其他网络，也不会在断开后仍启动 VPN。

### 3.4 可视化界面

- 基于 Windows 的图形化界面（GUI），界面语言为中文。
- 主要控件：VPN 软件列表、网络黑名单列表、“启动”按钮，以及添加/修改/删除相关控件。
- 启动软件时直接以 **管理员权限** 运行（弹一次 UAC 确认），以保证读取 SSID 与断开网络的权限稳定。

## 4. 数据与配置

- 使用本地 **JSON 文件** 持久化保存以下数据：
  - VPN 条目列表（显示名称、exe 完整路径）。
  - 网络黑名单列表。
- 程序重启后配置仍然保留。
- 配置文件损坏或缺失时，程序应回退到默认配置（网络黑名单仍含 `UIBE-WLAN`），并提示用户“配置已重置，已恢复默认值”，不应崩溃。

## 5. 非功能需求

- 运行环境：Windows 10 / 11，具备 WLAN 网卡（支持 WLAN API）。
- 权限：以管理员身份运行。
- 软件启动 VPN 后只负责本次启动，不做进程监控或关闭控制。

## 6. 技术选型

- **语言**：Python 3。
- **界面**：`tkinter`（标准库，轻量、无需额外依赖）。
- **WLAN 控制**：`ctypes` 调用 Windows WLAN API：
  - 读取当前连接 SSID：`WlanQueryInterface`（`wlan_intf_opcode_current_connection`）。
  - 断开当前 WiFi：`WlanDisconnect`。
- **配置**：标准库 `json`。
- **打包**：不需要打包，直接以 Python 脚本方式运行。

## 7. 开发计划（小步快跑）

每完成一步必须编写对应测试，**测试不通过不进入下一步**。

1. **项目骨架 + 配置读写**
   - 实现 JSON 配置（VPN 列表、黑名单）的保存与加载，缺失/损坏兜底。
   - 测试：保存→加载回读一致；文件缺失/损坏时能正确兜底。
2. **GUI 主窗口 + VPN 列表管理**
   - 实现 VPN 条目的添加/修改/删除/选中，以及黑名单管理。
   - 测试：增删改后界面与配置文件一致。
3. **读取当前 WiFi SSID**
   - 实现读取当前连接 WiFi 的 SSID；未连接时返回“无”（程序内以 `None` 表示）。
   - 测试：能读到当前 SSID；未连接时返回预期值。
4. **断开指定 WiFi**
   - 实现断开指定 SSID 的连接。
   - 测试：能断开指定 SSID，断开后状态正确。
5. **“启动”按钮完整逻辑**
   - 集成“读 SSID → 判断黑名单 → 拦截/启动”。
   - 测试：命中黑名单→不启动并弹窗；未命中或未连网→正常启动 exe。
6. **管理员权限 + 集成验收**
   - 确认 UAC 提权、整体流程跑通。

## 8. 已确认的决策记录

- 可以使用管理员权限（每次启动即请求）。
- 网络名称判定仅针对 **WiFi 的 SSID**。
- 界面为下拉框/列表，选中某个 VPN 条目后点击“启动”。
- 采用 **方案 A：纯拦截/提醒**（在校园网下只断开+提醒+不启动，不自动切换网络）。
- 多 VPN 条目共用一份全局网络黑名单。
- 未连接任何 WiFi（无 SSID）时，按“正常启动”处理。
- 软件启动 VPN 后不做进程监控。
- 不需要打包为独立 `.exe`，以脚本方式运行。
- 配置损坏/缺失时回退到默认配置（黑名单仍含 `UIBE-WLAN`），并提示用户配置已重置。
- 黑名单匹配采用精确匹配，SSID 区分大小写。
- VPN 条目允许多个，不强制去重，仅提示“建议显示名称唯一”。

## 9. 待确认/开放问题

暂无。所有核心决策已确认（含打包、名称去重、配置损坏兜底、SSID 大小写匹配）。
